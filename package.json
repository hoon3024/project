{
    "name": "management",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && yarn.cmd start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn.cmd server\" \"yarn.cmd client\""
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.16.4",
        "http-proxy-middleware": "^0.20.0",
        "mysql": "^2.17.1"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
